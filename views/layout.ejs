<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
    <title>Express Auth</title>
</head>
<body>
    
      <nav class="level has-background-black-ter" role="navigation" aria-label="main navigation">
        <div class="navbar-brand">
          <a class="level-item has-text-centered ml-6" href="/" align-self="center">
            <img src="https://i.imgur.com/sFn9ekz.png" alt="Piato: their recipes, your way" width="200px" height="100px">
          </a>
      
          <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
          </a>
        </div>

        <div class="level">
          <div class="level-item has-text-centered">
            <div class="buttons are-focused">
                <% if (!currentUser) { %>
                  <a class="button is-primary" href="/auth/signup">Signup</a>
                <% } else { %>
                  <a class="button is-danger is-light is-small" href="/auth/logout">Logout</a>
                <% } %>
              </a>
              <% if (!currentUser) { %>
                <a class="button is-success mr-6" href="/auth/login">Login</a>
              <% } else { %>
              <a class="button is-primary mr-6" href="/profile/<%=currentUser.id%>"><%= currentUser.name %></a>
              <% } %>
              </a>
            </div>
          </div>
        </div>
      </div>
      </nav>
    <div class="container is-desktop">
    <%- include('partials/alerts') %>
    <%- body %> 
    </div>
</body>
</html>